import React from 'react';
import Slider from '../Components/Slider';

const HomePage = () => {
    return (
        <div>
           <Slider></Slider>
        </div>
    );
};

export default HomePage;